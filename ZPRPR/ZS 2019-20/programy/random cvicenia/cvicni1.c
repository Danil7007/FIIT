
#include <stdio.h>

int main () {
    char a, d, c;
    
    scanf ("%c %c %c", &a, &b, &c) ;

    if ((a == b) && (a == c) && (b == c)) {
        printf ("Vsetky su rovnake") ;
    }

    else {
        if ((a == b) && (a == c) && (b == c)) {
            printf ("Ziadne sa nezhoduju") ;
        }
        {
            printf ("Dve su rovnake") ;
        }
    }
 
}

